[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ds6400"
version = "0.1.0"
description = "Shared code for the DS 6400 Quarto book."
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
